extends layouts/main.pug
include mixins/videoBlock

block content
    .vidoes 
        each video in videos
            +videoBlock({
                title:video.title,
                views:video.views,
                videoFile:video.videoFile
            })